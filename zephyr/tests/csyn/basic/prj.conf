CONFIG_ZTEST=y
CONFIG_CSYN=y
CONFIG_CSYN_NATIVE_UDP=n
CONFIG_ASSERT=y
# Tests default to -O0, which trips _FORTIFY_SOURCE on hardened host
# toolchains (nix); the suite has nothing to gain from unoptimized builds.
CONFIG_SPEED_OPTIMIZATIONS=y
